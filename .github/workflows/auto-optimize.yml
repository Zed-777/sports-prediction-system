name: Auto Optimize (weekly)

on:
  schedule:
    - cron: '0 3 * * 0'  # Every Sunday at 03:00 UTC
  workflow_dispatch: {}

jobs:
  optimize:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt || true
          pip install -r requirements_phase2.txt || true

      - name: Run tests
        run: |
          pytest -q

      - name: Run optimization sweep
        run: |
          python scripts/optimize_accuracy.py --mode backtest --league la-liga || true

      - name: Run historical backtest (VB-001)
        env:
          FOOTBALL_DATA_API_KEY: ${{ secrets.FOOTBALL_DATA_API_KEY }}
          SKIP_INJURIES: '1'
        run: |
          python scripts/run_backtest.py --days 365 --skip-predictor || true

      - name: Upload optimization artifacts
        uses: actions/upload-artifact@v4
        with:
          name: optimization-results
          path: |
            data/optimization_results/**
            reports/backtests/**
